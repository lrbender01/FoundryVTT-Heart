name: Test Build
run-name: ${{ github.actor }} testing a new push
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: '>=23.9.0'
      - run: npm ci
      - run: npm run grammar
      - run: npm run build
      - run: npm run update-system -- 0.X.Y